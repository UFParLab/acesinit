      common/basis_info/ntot_alpha, ntot_pcoeff, ntot_shells,  
     *                  Talpha, Tpcoeff, Tixalpha, Tixpcoeff 

      integer ntot_alpha, ntot_pcoeff, ntot_shells 
      integer maxcoeff, maxalpha, maxshells   
      parameter (maxcoeff = 100000)
      parameter (maxalpha = 100000)
      parameter (maxshells = 100000)
      integer Talpha(maxalpha)
      integer Tpcoeff(maxcoeff)
      integer Tixalpha(maxshells)
      integer Tixpcoeff(maxshells)

